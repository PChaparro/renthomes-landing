---
interface Props {
  as: "h1" | "h2";
  extraClasses?: string;
}

const { as: Element = "h1", extraClasses } = Astro.props;
---

<Element
  class:list={[
    "font-serif text-6xl lg:text-8xl text-balance text-brand-green capitalize",
    extraClasses
  ]}
>
  <slot />
</Element>

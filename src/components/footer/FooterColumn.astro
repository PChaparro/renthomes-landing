---
interface Props {
  title: string;
  extraClasses?: string;
}

const { title, extraClasses } = Astro.props;
---

<div class={extraClasses}>
  <span class="font-bold text-brand-green text-xl capitalize">{title}</span>
  <ul class="flex flex-col gap-4">
    <slot />
  </ul>
</div>
